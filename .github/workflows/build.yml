name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Install dependencies
      run: |
        sudo apt update -y
        sudo apt install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev
        pip install --user buildozer cython
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Setup Python path (for Buildozer)
      run: |
        sudo ln -s /usr/bin/python3 /usr/bin/python || true

    # O buildozer/python-for-android procura o sdkmanager no formato ANTIGO (tools/bin/sdkmanager),
    # que o Google não inclui mais nos pacotes atuais (só vem em cmdline-tools/).
    # Baixamos o pacote legado que ainda mantém essa estrutura, no caminho exato esperado.
    - name: Install legacy sdkmanager (tools/bin path)
      run: |
        mkdir -p $HOME/.buildozer/android/platform/android-sdk
        cd $HOME/.buildozer/android/platform/android-sdk
        wget -q https://dl.google.com/android/repository/sdk-tools-linux-4333796.zip -O sdk-tools.zip
        unzip -q sdk-tools.zip
        rm sdk-tools.zip

    # Aceita as licenças do Android SDK no caminho CORRETO que o buildozer realmente usa
    - name: Pre-accept Android SDK licenses
      run: |
        mkdir -p $HOME/.buildozer/android/platform/android-sdk/licenses
        echo "24333f8a63b6825ea9c5514f83c2829b004d1fee" > $HOME/.buildozer/android/platform/android-sdk/licenses/android-sdk-license
        echo "84831b9409646a918e30573bab4c9c91346d8abd" > $HOME/.buildozer/android/platform/android-sdk/licenses/android-sdk-preview-license

    - name: Build APK
      run: |
        yes | buildozer -v android debug || buildozer -v android debug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Prometheus-APK
        path: bin/*.apk
